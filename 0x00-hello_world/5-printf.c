#include <stdio.h>
/**
 * main - Entry point where the code goes using printf function
 * Return: always returns value (0)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
