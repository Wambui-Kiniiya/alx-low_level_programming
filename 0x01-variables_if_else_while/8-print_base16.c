#include <stdio.h>

/**
 * main - prints all numbers of base 16
 * in lowercase
 * Return: value (0)
 */
int main(void)
{
	int num;
	char ch;

	for (num = 0; nm < 10; num++)
		putchar((num % 10) + '0');

	for (ch = 'a'; ch <= 'f'; ch++)
		putchar(ch);

	putchar('\n');

	return (0);
}
